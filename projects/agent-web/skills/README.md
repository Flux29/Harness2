# Agent skills

Drop SKILL.md-style skill directories here (harness `skill_directories`),
or point SKILLS_DIR elsewhere (e.g. ..\eval-optimizer\skills). An empty dir
is fine — it just silences the "Default skills directory" warning.
